// Use the system font stacks defined in globals.css. Loading Google fonts via
// `next/font/google` makes production builds depend on fonts.googleapis.com,
// which is unavailable in some CI and deployment environments.
export const fontVariables = "";
