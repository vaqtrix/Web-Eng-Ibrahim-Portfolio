# Contact form (optional — form falls back to a mailto reply if unset)
RESEND_API_KEY=
CONTACT_FROM_EMAIL=onboarding@resend.dev
CONTACT_TO_EMAIL=siddiqui.ibrahim003@gmail.com

# Used for sitemap / OG absolute URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
